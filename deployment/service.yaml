---
apiVersion: v1
kind: Service
metadata:
  name: ndk-dashboard
  namespace: default
  labels:
    app: ndk-dashboard
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: ndk-dashboard